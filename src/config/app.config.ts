const uniqueString = '_dR5h7CXasL_';

export const LOGO = {
  TEXT: ' Rick and Morty API search',
};

export const LOCALSTORAGE = {
  SEARCH: `${uniqueString}search`,
};

export const BUTTON = {
  SEARCH: 'search',
  THROW_ERROR: 'Throw an Error',
};

export const SPINNER = {
  TEXT: 'Loading...',
};
